version: '2.1'
services:
  block-explorer:
    container_name: block-explorer
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - services-tier
    ports:
      - $GLS_FRONTEND_HOST:$GLS_FRONTEND_PORT:$GLS_FRONTEND_PORT
    env_file:
      - .env

networks:
  services-tier:
